[package]
name = "custom-shim-plugin"
version = "0.1.0"
edition = "2021"
publish = false
description = "Example shared library plugin for shim-plugin"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
